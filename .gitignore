# lokale secrets & zugangsdaten
.env

# ECHTE VEREINSDATEN - bleiben lokal, nie auf GitHub!
data/

# alter code - kann secrets enthalten
old/

# echte fotos / hochgeladene bilder (personen!)

# diverse
*.log
.DS_Store
Thumbs.db

# fotos ohne personen-check
front.jpg
hintergrund.png
39fb9c8d57757a4b24a9738eef839176.png

# setup-lock (wird bei installation erzeugt)
install.lock
